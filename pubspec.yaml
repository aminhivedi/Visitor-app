name: visito_mvp
description: MVP for Visito — Flutter + Firebase app for field sales/visitors
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.10.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.7.0
  firebase_storage: ^11.1.0
  firebase_messaging: ^14.3.0
  provider: ^6.0.5
  google_maps_flutter: ^2.4.0
  geolocator: ^9.0.2
  flutter_local_notifications: ^13.0.0
  cached_network_image: ^3.2.3
  connectivity_plus: ^4.1.0
  uuid: ^3.0.6
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
